pipeline {
    agent any

    stages {
        stage('test-pipeline') {
            steps {
                echo "test pipeline"
            }
        }
    }
    stages {
        stage('terraform init') {
            steps {
                sh 'terraform init'
            }
        }
    }
}
