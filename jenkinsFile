pipeline {
    agent any

    stages {
        stage('test-pipeline') {
            steps {
                echo "test pipeline"
            }
        }
        stage('terraform init') {
            steps {
                sh 'terraform init'
            }
        }
    }
}
